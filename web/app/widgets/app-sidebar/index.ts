// widgets/app-sidebar — public API of the sidebar navigation widget.
//
// Consumed by the default layout (app shell). Imports the current user from
// entities/user and the auth feature's logout flow via the session store.

export { default as AppSidebar } from './ui/AppSidebar.vue'
